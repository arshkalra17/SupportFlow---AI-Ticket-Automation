# Test-only dependencies — do NOT install in production.
# Install with: pip install -r requirements-test.txt
#
# Compatibility note:
#   fakeredis>=2.28 is required for redis-py>=8.x.
#   redis-py 8.x sends a HELLO handshake on every connection (RESP3 protocol).
#   fakeredis 2.26.x does not implement HELLO; 2.28+ does.
#   fakeredis>=2.38 is pinned as the latest verified compatible release.
#   Production redis-py is unchanged.

pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-xdist>=3.6.0
fakeredis>=2.38.0
email-validator>=2.0.0
